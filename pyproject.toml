[project]
name = "cv-platform"
version = "0.1.0"

[tool.setuptools]
packages = ["app", "services", "database", "prompts", "utils"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
